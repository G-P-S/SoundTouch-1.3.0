CFTempoChange - Simple filter to count number of samples passed thru per second.

Reports information with ICFTempoChangeStats::GetSampleStats(start, stop, sampleCount);
Or, if you hold down ctrl-shift when stopping (or pausing) the graph a message 
box will be displayed with the sample rate, samples / second.

